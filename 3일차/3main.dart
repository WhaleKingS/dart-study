void main() {
  List<int> list = [1, 2, 3, 4, 5];

  //<>안에 실수 변수 정수인지 정하기 가능 <>없어도됨
  //list.insert(0, 6); 맨 앞에 들어감
  //list.add(7); 맨 마지막에 들어감
  //list.addAll([8,9,10]); 맨 마지막에 리스트가 추가됨
  //list.sort(); 오름차순으로 정렬시켜줌
  //list.clear(); 싹다 삭제

  print(list);
}
